source "https://rubygems.org"

# GitHub Pages pinned environment
gem "github-pages", group: :jekyll_plugins

# Needed for Ruby 3+ local serving
gem "webrick", "~> 1.8"

# Keep plugins inside the jekyll_plugins group so Jekyll loads Liquid tags
group :jekyll_plugins do
  gem "jekyll-scholar", "~> 5.16"
end
